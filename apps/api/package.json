{
  "name": "@wison/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --env=\"\" --outdir dist",
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787 --var DEMO_AUTH_ENABLED:true --var DATABASE_URL:${DATABASE_URL:-postgresql://$USER@127.0.0.1/hank_platform_test}",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:db": "vitest run --config vitest.integration.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@wison/contracts": "^0.1.0",
    "hono": "^4.12.32",
    "jose": "^6.2.5",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "@types/pg": "^8.20.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.115.0"
  }
}
